<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RinceDCS.Views.AddStickImageDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RinceDCS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Joystick:" VerticalAlignment="Center" Margin="2"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{x:Bind ViewModel.Stick.AttachedJoystick.Name}" TextWrapping="Wrap"/>

        <TextBlock Grid.Column="0" Grid.Row="2" Text="Image Title:" VerticalAlignment="Center" Margin="2"/>
        <TextBox   Grid.Column="1" Grid.Row="2" Name="NewImageTabTitle" Text="{x:Bind ViewModel.NewImageTabTitle, Mode=TwoWay}"  TextChanging="NewImageTabTitle_TextChanging" MinWidth="200"/>
    </Grid>
</Page>
