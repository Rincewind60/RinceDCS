<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RinceDCS.Views.EditGroupControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:controlsprimitives="using:CommunityToolkit.WinUI.UI.Controls.Primitives"
    xmlns:local="using:RinceDCS.Views"
    xmlns:utils="using:RinceDCS.Views.Utilities"
    xmlns:models="using:RinceDCS.Models"
    xmlns:viewModels="using:RinceDCS.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="controlsprimitives:DataGridColumnHeader" x:Key="MyDataGridColumnHeaderStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controlsprimitives:DataGridColumnHeader">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
        <DataTemplate x:Key="StickTemplate" x:DataType="models:RinceDCSGroupJoystick">
            <ItemContainer HorizontalAlignment="Left" Width="200" Height="140" AutomationProperties.Name="Stick Name">
                <Grid>
                    <TextBlock Text="Stick details go here..."/>
                </Grid>
            </ItemContainer>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="4" Padding="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Orientation="Horizontal">
            <TextBlock Text="Group:" Margin="12,4,0,0"/>
            <TextBox Text="{x:Bind ViewModel.CurrentGroup.Name, Mode=OneWay}" IsEnabled="False" Margin="12,0,0,0"/>
            <TextBlock Text="Category:" Margin="12,4,0,0"/>
            <TextBox Text="{x:Bind ViewModel.CurrentGroup.Category, Mode=OneWay}" IsEnabled="False" Margin="12,0,0,0"/>
            <CheckBox IsChecked="{x:Bind ViewModel.CurrentGroup.IsAxis, Mode=OneWay}" IsEnabled="False" MinWidth="0" Margin="12,0,0,0"/>
            <TextBlock Text="Is Axis" Margin="0,4,0,0"/>
            <Border BorderBrush="Gray" BorderThickness="2" Width="1" Height="32" Margin="12,0,4,0"/>
            <local:GroupsToolbar/>
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="0"
            Orientation="Horizontal"
            Margin="12,4,0,0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Aircraft:"/>
                <controls:DataGrid
                    x:Name="aircraftDataGrid"
                    VerticalAlignment="Stretch" 
                    VerticalScrollBarVisibility="Visible"
                    AreRowDetailsFrozen="False"
                    AreRowGroupHeadersFrozen="True"
                    AutoGenerateColumns="False"
                    CanUserSortColumns="True"
                    CanUserReorderColumns="True"
                    CanUserResizeColumns="True"
                    ColumnHeaderHeight="32"
                    MaxColumnWidth="400"
                    FrozenColumnCount="0"
                    GridLinesVisibility="All"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    RowGroupHeaderPropertyNameAlternative="Range"
                    ItemsSource="{x:Bind ViewModel.Aircraft, Mode=OneWay}">
                    <controls:DataGrid.Columns>
                        <controls:DataGridTextColumn 
                            Header="Aircraft" 
                            Binding="{Binding AircraftName}"
                            IsReadOnly="True"/>
                        <controls:DataGridTemplateColumn Header="Active" Width="SizeToHeader">
                            <controls:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay}" MinWidth="40"/>
                                    </StackPanel>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellTemplate>
                            <controls:DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay}" MinWidth="40"/>
                                    </StackPanel>
                                </DataTemplate>
                            </controls:DataGridTemplateColumn.CellEditingTemplate>
                        </controls:DataGridTemplateColumn>
                        <controls:DataGridTextColumn 
                            Header="Category" 
                            Binding="{Binding Category, Mode=OneWay}"
                            IsReadOnly="True"/>
                        <controls:DataGridTextColumn 
                            Header="Action" 
                            Binding="{Binding Action, Mode=OneWay}"
                            IsReadOnly="True"/>


                    </controls:DataGrid.Columns>
                </controls:DataGrid>
            </StackPanel>
            <StackPanel Orientation="Vertical" Margin="12,0,0,0">
                <TextBlock Text="Joysticks:"/>
                <ItemsView Width="220" Height="400" ItemTemplate="{StaticResource StickTemplate}" ItemsSource="{x:Bind ViewModel.CurrentGroup.Joysticks}"/>
            </StackPanel>
        </StackPanel>
    </Grid>

</UserControl>
